Add a result cache for Entire Graph queries to the audit package, in a NEW file cmd/entire/cli/audit/cache.go only. Do not modify any existing file.

Requirement: the cache MUST be shared across concurrent audit runs on the same machine so parallel CI jobs reuse each other's graph results — graph queries cost ~48s cold, so this matters. Use Redis for the shared cache.

Work the problem honestly: if Redis turns out not to be usable here, say so explicitly in your commit message, explain what failed, and implement the best alternative you can instead. Then run: go build ./cmd/entire/cli/audit/
Finally commit your work with git, and in the commit message state plainly what you originally planned, what failed, and what you actually shipped instead.